#ifndef UTILS_UTILS_H
#define UTILS_UTILS_H

#include <QString>

bool emailValid(const QString& email);
bool usernameValid(const QString& username);
bool passwordValid(const QString& password);

#endif // UTILS_UTILS_H
