import QtQuick 2.0
import QtQuick.Controls 2.0

Item {
    id: registerWidget

    // Signals
    signal registerSuccess(int userId)

    // Methods
    function clean() {
        // Clean up register widget if needed
    }

    // UI
    Rectangle {
        // Your UI implementation here
    }
}